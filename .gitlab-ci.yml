image: golang:1.16.3

stages:
 - build
 - release

before_script:
  - go mod download
  - make bootstrap

build:
  stage: build
  script:
    - make build
    - ls -R pkg

release:
  stage: release
  script:
    - ./scripts/build.sh
#  only:
#    - tags
#    refs:
#      - master
