image: golang:1.16.3

stages:
 - test
# - build
# - release

before_script:
  - go mod download
  - make bootstrap
  - apt-get update -y
  - apt-get install zip -y

build:
  stage: test
  script:
    - make build
    - ls -R pkg

release:
  stage: test
  script:
    - chmod 770 ./scripts/build.sh
    - ./scripts/build.sh
#  only:
#    - tags
#    refs:
#      - master
